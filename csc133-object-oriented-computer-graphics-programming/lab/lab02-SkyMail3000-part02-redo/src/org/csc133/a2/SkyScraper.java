/**
 * SkyScraper is a fixed object. All SkyScrapers have the same size (chosen by
 * you), assigned when they are created (e.g, size of all SkyScrapers can be
 * 10). SkyScrapers are not allowed to change color once they are created.
    // All SkyScrapers have the same size (chosen by you), assigned when they are
    // created (e.g, size of all SkyScrapers can be 10). SkyScrapers are not allowed
    // to change color once they are created.

 */
public class SkyScraper extends Fixed {
    // Each SkyScraper is a numbered marker that acts as a waypoint on the flight
    // path
    private int sequenceNumber;
}